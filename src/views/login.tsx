import React, { useRef, useEffect, useState, } from 'react';

import './login.less'

const Login: React.FC<any> = (props) => {
  return (
    <React.Fragment>
      <div className="login">hello alvin ---</div>
    </React.Fragment> 
  )
}

export default Login